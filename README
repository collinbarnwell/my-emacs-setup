# Clone this repo.

git clone https://github.com/collinbarnwell/my-emacs-setup.git .emacs.d

# Clone (or comment out) all the necessary plugin repos.

cd ~/.emacs.d/plugins

git clone https://github.com/jaypei/emacs-neotree.git neotree

# Keep trying

emacs --debug-init nofile.txt &

# until it works.